# H:\Code 工作规则

## 目录用途

`H:\Code` 是用户在当前 Windows 电脑与另一台 Mac 电脑之间同步重要文件的目录。同步依赖 Git 和自动化脚本。目录中可能包含重要文稿、研究资料、代码、日志，以及尚未纳入 Git 的文件。

## 最高优先级：保护用户数据

- 把防止文件丢失、覆盖、误删和同步冲突放在首位。
- 不得因为文件未被 Git 跟踪，就认为它可以被删除、覆盖、移动、重命名或忽略。
- 不得擅自修改与当前任务无关的文件，尤其是文稿、数据、图片、研究资料和其他非 Git 文件。
- 用户要求诊断或检查时，默认只执行只读操作；除非用户同时明确要求修复或修改。
- 修改同步脚本时，只改与任务直接相关的脚本或配置，避免波及同步目录中的内容文件。

## 操作前检查

- 在执行会改变状态的操作前，先检查 Git 状态、当前分支、冲突、未提交改动和未跟踪文件。
- 精确确认操作路径和影响范围，不对整个 `H:\Code` 或含糊、未解析的路径执行递归修改、移动或删除。
- 如果发现合并冲突、进行中的 Git 操作、异常的大量文件变化，或结果可能覆盖任一电脑上的版本，应停止变更并向用户说明。
- 现有修改默认属于用户。不得覆盖、还原或顺手整理与任务无关的改动。

## Git 与同步安全

- 未经用户明确授权，不执行可能丢失历史或工作区内容的操作，包括但不限于：
  - `git reset --hard`
  - `git clean`
  - 强制 checkout/restore 覆盖文件
  - rebase、强制推送或删除分支
  - 丢弃本地修改或自动选择冲突一方
- 不自动解决内容冲突。应先展示冲突文件和风险，由用户决定保留或合并方式。
- 在 pull、merge、rebase、commit 或 push 前，先确认当前状态适合该操作。
- 不把“同步成功”等同于命令退出成功；应在安全范围内核对提交、远端分支和目标文件状态。
- 若可能同时存在 Windows 与 Mac 两边的新版本，优先保留双方副本，再讨论合并。

## 写入、删除与恢复

- 任何可能删除、覆盖、批量重命名、批量移动或大范围改写文件的操作，都必须事先说明精确目标、影响和恢复方案，并获得用户明确同意。
- 能采用可恢复方式时，优先采用备份、复制或其他可回退方案。
- 不得使用脚本、通配符或未验证的变量对大量文件执行破坏性操作。
- 创建临时文件时放在明确的临时目录，不混入用户文稿目录。

## 沟通与交付

- 执行前说明将检查或修改的范围。
- 如果需要扩大任务范围、处理用户文件或采取有覆盖风险的方案，先征得用户同意。
- 完成后明确列出：检查了什么、修改了什么、没有修改什么、验证结果及剩余风险。
- 若安全完成任务所需信息不足，宁可暂停并询问，也不要猜测性地处理用户数据。

本文件适用于 `H:\Code` 及其所有子目录。更深层目录中的 `AGENTS.md` 可以补充更具体的规则，但不得降低这里的数据保护要求。
