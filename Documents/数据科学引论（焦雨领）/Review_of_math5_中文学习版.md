# Review_of_math5 中文学习版

> 来源：`Review_of_math5.pdf`  
> 页数：56  
> 说明：这是按页整理的中文导读/学习笔记，不覆盖原 PDF。公式和英文术语尽量保留，中文部分负责解释它们在课程中的作用。

这份 review 主要补充线性代数、矩阵分析、凸优化、次微分、对偶和 KKT 等证明材料。

建议读法：先看每页的“本页导读”，再回到原 PDF 看公式和图；遇到证明页，优先抓证明思路和用到的工具。

## 第 1 页：补充证明复习

原始标题：`Review — Some Supplementary Proofs`

本页导读：

- 本页主要是在引入或连接一个课程概念。阅读时先识别对象是什么、假设是什么、结论是什么。
- 如果有公式，建议把它拆成三部分：左边表示要研究的量，右边表示可计算或可估计的量，条件说明公式何时成立。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

## 第 2 页：中文导读

原始标题：`Notation`

本页导读：

- 范数衡量大小，内积衡量角度和投影；很多优化结论都依赖范数与对偶范数。
- 有限维空间中范数等价，但具体常数会影响算法和误差界。

## 第 3 页：最佳低秩近似

原始标题：`Best low rank approximation — proof`

本页导读：

- SVD 把矩阵分解为正交方向和奇异值，是低秩近似、PCA、压缩和条件数分析的基础。
- 最佳低秩近似定理说明：保留最大的若干奇异值，就是谱范数或 Frobenius 范数下的最优低秩近似。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

## 第 4 页：最佳低秩近似

原始标题：`Best low rank approximation — proof`

本页导读：

- SVD 把矩阵分解为正交方向和奇异值，是低秩近似、PCA、压缩和条件数分析的基础。
- 最佳低秩近似定理说明：保留最大的若干奇异值，就是谱范数或 Frobenius 范数下的最优低秩近似。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

## 第 5 页：最佳低秩近似

原始标题：`Best low rank approximation — proof`

本页导读：

- SVD 把矩阵分解为正交方向和奇异值，是低秩近似、PCA、压缩和条件数分析的基础。
- 最佳低秩近似定理说明：保留最大的若干奇异值，就是谱范数或 Frobenius 范数下的最优低秩近似。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

## 第 6 页：最佳线性编码器与解码器

原始标题：`Corollary — HW: best linear encoder and decoder`

本页导读：

- 本页主要是在引入或连接一个课程概念。阅读时先识别对象是什么、假设是什么、结论是什么。
- 如果有公式，建议把它拆成三部分：左边表示要研究的量，右边表示可计算或可估计的量，条件说明公式何时成立。

## 第 7 页：条件数

原始标题：`Condition number`

本页导读：

- 条件数衡量线性系统对扰动的敏感程度，通常越大越病态。
- 它依赖所选范数，但在有限维空间里不同范数具有等价性。

关键词：`Condition number`

## 第 8 页：条件数

原始标题：`Condition number`

本页导读：

- 条件数衡量线性系统对扰动的敏感程度，通常越大越病态。
- 它依赖所选范数，但在有限维空间里不同范数具有等价性。

关键词：`Condition number`

## 第 9 页：范数

原始标题：`Schattenq-norms`

本页导读：

- Schatten 范数是把矩阵奇异值当向量后取向量范数。
- 核范数是奇异值的 `l1` 和，常作为矩阵秩的凸替代。

## 第 10 页：核范数

原始标题：`Nuclear norm — Lemma 1`

本页导读：

- Schatten 范数是把矩阵奇异值当向量后取向量范数。
- 核范数是奇异值的 `l1` 和，常作为矩阵秩的凸替代。

关键词：`Nuclear norm`

## 第 11 页：核范数

原始标题：`Nuclear norm — Lemma 2`

本页导读：

- Schatten 范数是把矩阵奇异值当向量后取向量范数。
- 核范数是奇异值的 `l1` 和，常作为矩阵秩的凸替代。

关键词：`Nuclear norm`

## 第 12 页：核范数

原始标题：`Nuclear norm`

本页导读：

- Schatten 范数是把矩阵奇异值当向量后取向量范数。
- 核范数是奇异值的 `l1` 和，常作为矩阵秩的凸替代。

关键词：`Nuclear norm`

## 第 13 页：导数

原始标题：`Derivatives`

本页导读：

- 矩阵函数的导数最好通过内积表示一阶变化：`df(X)[H] = <G, H>`。
- Frechet 导数强调对任意扰动方向的线性近似，是矩阵优化里最稳的语言。

## 第 14 页：导数

原始标题：`F- and G-derivatives`

本页导读：

- 矩阵函数的导数最好通过内积表示一阶变化：`df(X)[H] = <G, H>`。
- Frechet 导数强调对任意扰动方向的线性近似，是矩阵优化里最稳的语言。

## 第 15 页：中文导读

原始标题：`Example — ln(det(X))`

本页导读：

- 矩阵函数的导数最好通过内积表示一阶变化：`df(X)[H] = <G, H>`。
- Frechet 导数强调对任意扰动方向的线性近似，是矩阵优化里最稳的语言。
- 例子页的价值在于把抽象定义落地；复习时可以自己换一组简单数字检验结论。

## 第 16 页：凸性

原始标题：`Convex set`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 17 页：中文导读

原始标题：`Cone`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。
- 例子页的价值在于把抽象定义落地；复习时可以自己换一组简单数字检验结论。

## 第 18 页：中文导读

原始标题：`Elliptical ball`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 19 页：凸性

原始标题：`Convex functions`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 20 页：凸性

原始标题：`Properties of strongly convex functions`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

## 第 21 页：凸性

原始标题：`Convexity-preserving operations`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

## 第 22 页：凸性

原始标题：`Properties of convex functions`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

## 第 23 页：中文导读

原始标题：`Co-coercivity of∇f`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 24 页：次微分

原始标题：`Subdifferential definition`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

关键词：`Subdifferential`

## 第 25 页：范数

原始标题：`Recall normal cone`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 26 页：次微分

原始标题：`Subdifferential`

本页导读：

- 次梯度把不可微凸函数的一阶最优性条件推广到非光滑情形。
- Fermat 法则：凸优化问题的最优点满足 `0 in partial f(x*)`。
- 约束问题可以通过指示函数和 normal cone 转化为次微分条件。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

关键词：`Subdifferential`

## 第 27 页：次微分

原始标题：`Rules of computing subdifferential`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

关键词：`subdifferential`

## 第 28 页：次微分

原始标题：`Rules of computing subdifferential`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。
- 例子页的价值在于把抽象定义落地；复习时可以自己换一组简单数字检验结论。

关键词：`subdifferential`

## 第 29 页：中文导读

原始标题：`Example —∂∥x∥ 2 ={g`

本页导读：

- 本页主要是在引入或连接一个课程概念。阅读时先识别对象是什么、假设是什么、结论是什么。
- 如果有公式，建议把它拆成三部分：左边表示要研究的量，右边表示可计算或可估计的量，条件说明公式何时成立。
- 例子页的价值在于把抽象定义落地；复习时可以自己换一组简单数字检验结论。

## 第 30 页：中文导读

原始标题：`Fermat’s rule`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 31 页：中文导读

原始标题：`Calculation of arg min 1`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 32 页：凸性

原始标题：`Optimality condition for constraint convex optimization`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 33 页：次微分

原始标题：`Existence of subdifferential`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

关键词：`subdifferential`

## 第 34 页：次微分

原始标题：`Subdifferential monotonicity`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

关键词：`Subdifferential`

## 第 35 页：凸性

原始标题：`Lipschitz Convex Functions and Subgradients`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

关键词：`subdifferential`

## 第 36 页：Bregman 距离

原始标题：`Bregman divergence`

本页导读：

- Bregman 距离不是普通距离，而是由凸函数的一阶线性化误差定义。
- 它常用于镜像下降、广义投影和 KL 距离相关算法。
- 例子页的价值在于把抽象定义落地；复习时可以自己换一组简单数字检验结论。

关键词：`Bregman`

## 第 37 页：Bregman 距离

原始标题：`Projection under Bregman distance`

本页导读：

- 矩阵函数的导数最好通过内积表示一阶变化：`df(X)[H] = <G, H>`。
- Frechet 导数强调对任意扰动方向的线性近似，是矩阵优化里最稳的语言。

关键词：`subdifferential`、`Bregman`

## 第 38 页：Bregman 距离

原始标题：`Projection under Bregman distance`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

关键词：`Bregman`

## 第 39 页：Bregman 距离

原始标题：`Bregman distance: generalized pythagorean theorem/inequality`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

关键词：`Bregman`

## 第 40 页：中文导读

原始标题：`Legendre transformation`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

关键词：`Fenchel`

## 第 41 页：Fenchel 共轭

原始标题：`Fenchel conjugate: examples`

本页导读：

- Fenchel 共轭把函数表示为所有仿射下界的上确界，是凸分析和对偶理论的核心工具。
- 闭凸函数满足双共轭等于自身，这是很多对偶证明的基础。
- 例子页的价值在于把抽象定义落地；复习时可以自己换一组简单数字检验结论。

关键词：`Fenchel`

## 第 42 页：Fenchel 共轭

原始标题：`Fenchel conjugate: example`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。
- 例子页的价值在于把抽象定义落地；复习时可以自己换一组简单数字检验结论。

关键词：`Fenchel`

## 第 43 页：中文导读

原始标题：`fand its bi-conjugatef ∗∗`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

关键词：`Fenchel`

## 第 44 页：中文导读

原始标题：`fand its bi-conjugatef ∗∗`

本页导读：

- Fenchel 共轭把函数表示为所有仿射下界的上确界，是凸分析和对偶理论的核心工具。
- 闭凸函数满足双共轭等于自身，这是很多对偶证明的基础。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

## 第 45 页：Ito 积分 / Ito 公式

原始标题：`Quasi-convex function — definiton`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 46 页：凸性

原始标题：`First order condition for quasi-convex functions`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 47 页：中文导读

原始标题：`Log-concave second order condition`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 48 页：中文导读

原始标题：`Phase retrieval problem`

本页导读：

- 本页主要是在引入或连接一个课程概念。阅读时先识别对象是什么、假设是什么、结论是什么。
- 如果有公式，建议把它拆成三部分：左边表示要研究的量，右边表示可计算或可估计的量，条件说明公式何时成立。

## 第 49 页：次微分

原始标题：`Limiting subdifferential example`

本页导读：

- 次梯度把不可微凸函数的一阶最优性条件推广到非光滑情形。
- Fermat 法则：凸优化问题的最优点满足 `0 in partial f(x*)`。
- 约束问题可以通过指示函数和 normal cone 转化为次微分条件。
- 例子页的价值在于把抽象定义落地；复习时可以自己换一组简单数字检验结论。

关键词：`subdifferential`

## 第 50 页：次微分

原始标题：`Generalizations of subdifferential and their relationships`

本页导读：

- 生成式学习的目标是从样本中学习一个分布，并能产生新的样本。
- 扩散模型常把数据逐渐加噪到简单分布，再学习反向过程把噪声还原为数据。
- 注意区分三层对象：样本、分布、把一个分布推到另一个分布的映射或随机过程。

关键词：`subdifferential`

## 第 51 页：线性规划

原始标题：`Linear programming and its dual`

本页导读：

- KKT 条件把可行性、驻点条件、对偶可行性和互补松弛统一起来。
- 对凸优化，在适当约束资格条件下，KKT 通常也是最优性的充要条件。

## 第 52 页：凸性

原始标题：`KKT for convex constraint optimization`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。
- 这类证明页建议抓住证明路线，不必一开始背每一步：先看用到的工具，再看关键不等式或等式变形。

关键词：`KKT`

## 第 53 页：中文导读

原始标题：`Linearized feasible direction cone`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 54 页：中文导读

原始标题：`Constraint qualification`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

关键词：`KKT`

## 第 55 页：中文导读

原始标题：`References`

本页导读：

- 凸集的核心是线段封闭；凸函数的核心是函数图像下方的线性插值不超过函数值。
- 强凸性比普通凸性更强，会带来唯一解和更好的收敛性质。

## 第 56 页：中文导读

原始标题：`Thank you!`

本页导读：

- 本页主要是在引入或连接一个课程概念。阅读时先识别对象是什么、假设是什么、结论是什么。
- 如果有公式，建议把它拆成三部分：左边表示要研究的量，右边表示可计算或可估计的量，条件说明公式何时成立。

## 快速术语表

- Bregman 距离
- Fenchel 共轭
- Ito 积分 / Ito 公式
- 凸性
- 导数
- 最佳低秩近似
- 最佳线性编码器与解码器
- 条件数
- 核范数
- 次微分
- 线性规划
- 范数
- 补充证明复习
