flowchart TD
    A["输入：车型参数、货物数据"] --> B["初始化剩余货物集合"]

    %% 初始解
    B --> C["构造初始解（快速贪心逐车装载）"]

    %% 外层减车
    C --> D["外层：尝试减少车辆数量"]

    D --> E{"当前车辆数可行？"}

    %% 内层优化
    E -- 是 --> F["内层优化（单车装载提升）"]

    F --> G["GA搜索单车装载策略"]
    G --> G1["编码：装载顺序 / 行策略 / G3比例"]
    G1 --> G2["策略评估（装箱仿真）"]
    G2 --> G3["选择 / 交叉 / 变异"]

    G3 --> H["生成最优单车策略"]

    %% G3双通道
    H --> I["执行装箱（含G3双通道）"]
    I --> I1["通道1：底层铺放"]
    I --> I2["通道2：G1/G2顶面放置"]

    %% 重分配
    I --> J["重分配剩余货物"]
    J --> K["更新车辆装载方案"]

    %% 判断继续减车
    K --> D

    %% 不可行
    E -- 否 --> L["回退到上一步可行解"]

    L --> M["输出最优车辆方案"]