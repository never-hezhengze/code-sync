### 参数定义：
$Car$: 车型，$∈\{1,2\}$,取1表示车型1（轻型厢式货车）,取2表示车型2（中型厢式货车）
$(L_{Car}​,W_{Car}​,H_{Car})$: 对应车型的尺寸,其中$(L_1,W_1,H_1)=(420,210,220)$,$(L_2,W_2,H_2)=(680,245,250)$
$V_{Car},M_{Car}$: 对应车型的体积和载重,其中$M_1=6000kg,M_2=10000kg$
$CS_{Car}$: Cost per shipment,单次运输成本,$CS_1 = 450,CS_2 = 700$
$货物i \in \mathcal I$: 货物集合$\mathcal I = \{1,2,...,n\}$
$货物类型向量T$: $(t_1,t_2,...,t_n)$
$货物i的初始尺寸$: $(l_i^0,w_i^0,h_i^0),v_i = l_i^0*w_i^0*h_i^0,重量为m_i$
$货物i的放置姿态r$: $\in \Omega = \{1,2,3,4,5,6\}$共6种姿态,对每种姿态定义矫正矩阵$\Delta_r$,默认取1
$货物i的实际尺寸(l_i^r,w_i^r,h_i^r)$: $\Delta_r (l_i^0,w_i^0,h_i^0)$

### 决策变量:（哪些变量可以完成对装箱方案的一对一映射）
$位置变量$: $(x_i,y_i,z_i)$定义其在车厢中的右后下角坐标,$(x_i+l_i^r,y_i+w_i^r,z_i+h_i^r)代表其左前上角坐标$
$姿态变量R$: $(r_1,r_2,...,r_n)$
$0-1变量\eta_i$: $\eta_i$取1代表货物$i$放入车厢中,取0代表不放入
$车辆使用数K$: 车辆集合为 $\mathcal K=\{1,2,\dots,K\}$
$\lambda_{ik}=
\begin{cases}
1,& \text{货物 }i\text{ 被分配到第 }k\text{ 辆车}\\
0,& \text{否则}
\end{cases}$
$y_k=
\begin{cases}
1,& \text{第 }k\text{ 辆车被启用}\\
0,& \text{否则}
\end{cases}$



# 问题一

### 约束条件:
 1. 
 2.
 3.


## 问题1.1（以单车满载率为目标）
### 目标函数:
- 以车型一为对象：
$$ Z_1 = max(\frac{\sum_{i=1}^{n}m_{i}\cdot \eta_{i}}{M_{1}}),s.t.约束条件P_1$$  

- 以车型二为对象：
$$ Z_2 = max(\frac{\sum_{i=1}^{n}m_{i}\cdot \eta_{i}}{M_{2}}),s.t.约束条件P_2$$

$Z = max(Z_1,Z_2)$

## 问题1.2（把附件 1 中的货物装完且所用车辆最少）
### 目标函数:
$$N = min\sum_{k\in\mathcal K} y_k$$

## 
### 约束条件
- 满足每件货物必须且仅能装入一辆车：
$$sum_{k\in\mathcal K}\lambda_{ik}=1,\qquad \forall i\in\mathcal I$$


附件一内容：
```docx
一、基本数据与假设
1. 货车箱体参数（两种标准车型）
- 车型1（轻型厢式货车）
内尺寸：长 420cm × 宽 210cm × 高 220cm
额定载重：6000kg
单次运输成本：450 元/趟（含油费、过路费、人工）
- 车型2（中型厢式货车）
内尺寸：长 680cm × 宽 245cm × 高 250cm
额定载重：10000kg
单次运输成本：700 元/趟
2. 货物类型与基础约束
现有一批待运货物，分为三类：
（1）标准件：长方体纸箱，可任意方向摆放，可堆叠，无特殊约束；
（2）易碎件：仅允许单层放置，不可堆叠，摆放时底面必须完全贴合箱体底面或其他标准件顶面；
（3）定向件：有明确方向要求（如“向上”标识），仅允许一种摆放姿态，可堆叠但上层货物重心不得超出下层投影范围。
所有货物均为刚性长方体，不可挤压、不可拆分，装箱时货物之间、货物与箱体之间不得重叠，必须保持正交摆放（棱与箱体棱平行）。
3. 附加约束
（1）承重约束：下层货物承重上限为 500kg/平方米，超重则不允许堆叠；
（2）稳定性约束：货物高度不得超过车厢高度，且顶层货物距车厢顶部预留不小于 3cm 安全间隙。
二、任务数据
现有某批次货物参数信息如下（单位：cm，kg）：
货物编号	类型	长×宽×高	单重	数量
G1	标准件	60×40×30	12	800
G2	标准件	50×35×25	8	1000
G3	易碎件	70×50×40	15	300
G4	定向件	80×60×50	25	400
G5	定向件	40×40×60	18	500
```