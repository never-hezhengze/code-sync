% 矩阵化写法，利用距离公式

load mat_replace.mat

tic

X2 = sum(X.^2, 2);  % 计算每个点的平方和，得到100x1列向量

% 计算成对欧几里得距离的矩阵
% 利用距离公式：||a-b||^2 = ||a||^2 + ||b||^2 - 2*a·b
% X2 + X2' 得到每对点的平方和之和（100x100矩阵）
% X*X' 得到所有点对的点积（100x100矩阵）
D3 = sqrt(X2 + X2' - 2 * (X * X'));  % 对每对点计算欧氏距离

toc

isequal(D,D3)